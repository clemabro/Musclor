import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        marginTop: 25,
    },
    title: {
        fontSize: 24,
        fontWeight: "bold",
        marginLeft: 25,
    },
    liste: {
        marginBottom: 70,
    },
});

export default styles;