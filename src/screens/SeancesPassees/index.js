import React from 'react';
import { Text, View } from 'react-native';
import styles from './styles';

const SeancePasseesScreen = (params) => {
    
return (
    <View>
        <Text>SeancePasseesScreen</Text>
    </View>
)};

export default SeancePasseesScreen;
